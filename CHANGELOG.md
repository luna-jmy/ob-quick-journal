# Changelog

本项目的所有重要改动都记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 0.4.0

### 新增

- **「段落」标题区类型**：整段自由文字（非列表、非内联字段），一天一条、内容可长；
  已有内容时覆盖前确认；支持速记面板（聚合显示 + 输入条直发）
- **自动时间戳**：列表 / 段落标题区在面板开启后可选——写入自动加 `HH:mm` 前缀，
  速记面板解析并在每条记录旁显示时间；面板未开启时该选项禁用
- **速记面板条目就地编辑 / 删除**：每条记录带编辑、删除、跳转三个按钮——
  列表行原位改写（保留列表标记与时间戳）、字段条目改值 / 清值、段落整段重写 / 清空；
  行级写回前做原文校验，笔记已变化时提示刷新而不是写错行

### 修复

- 多行插入顺序算法重写（同锚点连排、标题与首行相邻），段落 / 补建行落的
  位置不再受原空行干扰

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.3.0

### 新增

- **速记面板**（命令「打开速记面板」，Thino 式内容流）：顶部输入条直发到列表标题区
  （Enter 发送 / Shift+Enter 换行），下方按天聚合「开启内容汇总面板」的文本 / 列表标题区内容
  （今天 / 昨天 / 日期分组，任务带状态符号，点击条目跳转当日日志）；近 7 / 30 天切换；
  日志文件变更防抖自动刷新
- **按标题区开启面板**：设置里文本 / 列表类型新增「开启内容汇总面板」开关
- **命令「打开快速录入」**：直接打开动作选择弹窗（可绑快捷键 / 移动端工具栏）
- **设置「恢复默认设置」**：确认后重置全部配置（只清 data.json，不动笔记内容）

### 说明

- 速记面板为原创实现（vanilla DOM），交互形态参考 Obsidian-Thino-Lite（MIT）；
  未复用其代码

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.2.0

### 变更

- **范围收缩为日日志快速录入**：移除周复盘动作与汇总视图里的复盘卡片（复盘功能另行开发）
- **标题区模型**：设置里以「日志标题」为配置单元——每个标题区选类型（打卡 / 数据 / 文本 / 列表），
  自定义标题与字段；无内联字段的标题区为列表，逐项追加内容（可配行模板，如 `- [ ] {{value}}`）
- **从模板识别**：设置里填模板笔记路径，一键按标题与内联字段重建标题区配置
- **打卡类型**：勾选式 ✔️/❌ 开关（不选不写），替代旧的「留空不写」脚本式提示——
  表单提示整体去 QuickAdd 化
- v0.1 配置自动迁移（registry.daily → 标题区；周/月与旧动作丢弃）

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.1.0

### 新增

- **快速录入（原型）**：6 个出厂动作（每日打卡 / 数据记录 / 今日小结 / 本周复盘 /
  加一条任务 / 记一条灵感）；表单按字段类型渲染（布尔三态钮 / 数字 / 文本 / 多行），
  留空不写、覆盖前确认；写入走 `Vault.process` 原子替换，定位不到字段行时按注册表补建。
- **目标笔记自动创建**：按字段注册表生成骨架（frontmatter + 标题 + 空值字段行），
  与现有 TPL-* 日志模板同构。
- **日志汇总视图（原型）**：周 / 月 / 年期间切换；任务完成 / 新建统计、打卡率比例条、
  数值字段均值与区间；周视图可打开 / 创建本周复盘笔记。
- **i18n**：中英双语（跟随 Obsidian，可手动切换），`check:i18n` 校验键一致性。
- 解析 / 期间 / 定位 / 聚合纯函数层 + vitest 单测（含骨架 round-trip）。

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。
