# Changelog

本项目的所有重要改动都记录在这里。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## 0.10.0

### 变更

- **滚动按钮图标改为 calendar-clock**（原 arrow-down-up 是排序语义）
- **列表条目不再支持 Shift+Enter 换行**（多行内容面板无法按行识别），提示文案随目标
  类型切换；发送时粘贴的多行自动压成单行——只有段落保留换行
- **条目归档**：列表/任务条目新增 archive 按钮，行尾追加 `[archive:: true]`
  （dataview 内联字段，可被外部查询识别），带标识的条目不再显示在速记面板
- **未完成任务标识设置简化**：空格为隐含标配不再体现在设置里，只填额外字符
  （默认 `>`；旧配置中的空格自动剔除）
- **设置页布局重构**（参考 Project Master）：分「通用 / 日志 / 速记面板」三个 tab，
  一类一页、记住所在页；日志 tab 内保留日/周/月/年切换

## 0.9.0

### 新增

- **未完成任务滚动按钮**（速记面板顶部）：从最近一期含未完成任务的日日志把任务块
  （含缩进子步骤）搬到今天的任务区，源文件原子删除——语义对齐 journal_tasks_rolling.js
  脚本；点击后先预览来源与数量，确认再迁移
- **未完成标记可配置**：设置里「未完成任务标识」支持多个勾选框字符（逗号分隔，
  `␣` 表示空格），默认 `␣,>` 与脚本一致；可加 `/`、`-` 等
- **「显示已完成任务」成为持久设置**（速记面板偏好，默认开）；面板眼睛按钮改为
  切换该设置
- **录入区重排**：卡片式 composer——目标行 / 自增高输入框（setCssProps）/
  底部「Enter 发送 · Shift+Enter 换行」提示与发送按钮；窄屏适配同步优化

### 未验证

- 滚动功能在真实 vault 的表现（含跨多天回溯、源文件清理）；移动端 composer 手感——
  欢迎反馈。

## 0.8.0

### 变更

- **查询只保留 dataview / dataviewjs**：Tasks 插件没有公开查询 API，按决定移除 tasks
  查询类型（已保存的 tasks 查询在加载配置时自动清理）；dataview 走官方
  `dataview.api`（executeJs / tryQueryMarkdown + MarkdownRenderer，CW 同款）
- **速记面板顶部可收起**：常驻一条细栏（箭头 + 展开/收起），收起后工具栏与输入区
  隐藏，内容流立即顶上来——手机端查看不再被顶部功能区挤压

## 0.7.0

### 变更

- **查询组件去掉全部预设**：不再自动识别日志里的查询块，只渲染用户手动添加的
  （此前日志里的大量查询被当作原文显示，正是问题来源）
- **tasks 查询改原生执行**：Tasks 插件没有任何公开的查询执行 API（主分支仅 apiV1），
  常用筛选子集改为插件内实现——`path / filename includes（not）`、`tags include`、
  `done / not done`、`status.type is (not)`；出现不支持的行整体降级说明（不静默忽略）；
  结果可点击跳转源笔记
- **dataview / dataviewjs 对齐 Custom Workspace 的做法**：`executeJs`（dataviewjs）与
  `tryQueryMarkdown` + MarkdownRenderer（dataview），探测不到显示需要 Dataview

### 未验证

- 原生 tasks 子集在大库上的性能（查询请带 `path includes` 缩小范围）；
  dataview 委托渲染在真实 vault 的表现——欢迎反馈。

## 0.6.4

### 修复

- **段落写回打散内容**（根因：覆盖重建用递增锚点，删除旧行后位置位移，多行段落的
  中后段被插到下一个标题后面）——重建行改为共用标题锚点、按序连排，回归测试钉死落位
- **段落读取不再压缩结构**：段内空行原样保留，编辑弹窗所见即笔记原文，写回不改结构

## 0.6.3

### 修复

- **查询块渲染改走官方处理器管线**：不再探测 Dataview / Tasks 的具体 API 形状
  （v3 返回值等猜测不可靠），改为把查询包回 ` ```dataview / dataviewjs / tasks ` 围栏
  交给 `MarkdownRenderer`——由对应插件自己注册的代码块处理器渲染，只做「插件是否启用」
  的探测用于降级说明
- **趋势图选字段即时刷新**：下拉切换后重绘，不再需要手动刷新

## 0.6.2

### 修复

- **tasks 查询渲染**：Tasks API v3 的 `executeTasksQuery` 返回任务数组，桥此前只认
  字符串 / `{tasks}` 形态导致误判失败；数组结果按 `toMarkdown()` 序列化渲染
- **数据趋势图**：加 X 轴（首/中/尾日期）与 Y 轴（最大/中值/最小 + 网格虚线 + 轴线）；
  数据点画圆点；全等值画居中平线（不再贴底一条死线）；右侧最新/最大/最小读数
- **全年热力图正方形**：行轨道改 `max-content`，格子由 `aspect-ratio` 定高
- **设置页跳顶**：结构性重绘前记住滚动位置、画完恢复，改任何一项不再跳回顶部

## 0.6.1

### 修复

- **快速录入恢复顶端整行条**（不再卡片化，也不进编辑布局）
- **卡片等高**：同一行卡片拉伸到等高；卡片与热力图格加 `min-width: 0 / overflow` 防护，
  周视图热力图不再溢出
- **周视图月历与月视图同构**（整月网格 + 当前周格子高亮）；顺带修掉月初跨界周
  因 UTC 日期解析锚错月份的问题
- **自定义查询渲染**：手工查询补上源笔记上下文（回退到当天日日志）；Tasks 探测
  增加全局 `tasksApiV3` 入口，`{ tasks: [...] }` 结果序列化为 markdown 渲染

## 0.6.0

### 修复

- **段落重发进入编辑态**（上一版预填失效的根因：取错了字段）；设置里段落类型不再出现
  「添加字段」编辑器
- 速记面板窄屏布局：工具栏换行、搜索框整行、发送按钮贴右、操作按钮常显
- 月度柱状图基线不齐：绘图区与标签行分离，固定高度对齐

### 新增

- **日志汇总编辑模式**（工具栏齿轮）：卡片拖拽排序、删除、底部「添加组件」补齐；
  不调宽度，自动排版不变
- **多期间日志**：设置按 日/周/月/年 分 tab——各自目录与标题区；周/月/年复盘为对应类型
  下的文本标题区（默认按 TPL 字段预置），命令调用无独立图标；汇总的快速录入条含全部类型；
  月历的年/月片与周号列可点开对应复盘笔记（不存在则按该类型建骨架）
- **热力图随期间自适应**：周 = 7 大格（星期 + 数量）、月 = 日历格（日号 + 数量）、
  年 = 小格整行展开；任务完成与内容记录两张图同一模式
- **数据趋势单选**：下拉选一条字段折线（不同量纲不混画）
- **查询块手工录入**：编辑模式下可添加/删除自定义查询（dataview / dataviewjs / tasks），
  与日志内自动识别的合并渲染
- 任务指标行并入任务图卡（不再单列）；月历默认不出现在年视图
- **默认打开位置**：设置里可选日志汇总 / 速记面板开在标签页还是右侧边栏
- v0.5 扁平配置自动迁移到按类型组织

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下图表与月历样式、查询块在装/未装
  Dataview 与 Tasks 时的降级展示、编辑模式拖拽在触屏上的表现——均未在真实 vault
  验证，欢迎反馈。

## 0.5.1

### 修复

- 速记面板任务条目的状态符号**只保留在切换按钮上**：正文不再重复渲染 ☐/☑（此前按钮与
  正文各一个方框，分不清点哪个）；按钮加大点击区并加 hover 反馈；汇总「最近速记」小卡
  无按钮，由渲染层按任务状态补符号

## 0.5.0

### 新增

- **速记面板**：
  - 标题筛选下拉**同时控制输入目标与展示范围**（选中文本字段区时输入条隐藏）；
  - 任务条目可**点击状态符号直接完成/取消**（完成自动补 `✅ 日期`）；
  - 列表条目新增**任务/列表互转**按钮（`- 内容` ↔ `- [ ] 内容`）；
  - **显示已完成**开关 + **搜索栏**（内容 / 展示名 / 标题名模糊匹配）；
  - **段落重发 = 进入编辑态**：面板直发与捕获弹窗都预填当天现有内容，提交即整段重写，
    不再弹覆盖确认
- **日志汇总组件化**（参考 ob-workspace 组件形态，图表全部原生 SVG/CSS 自绘）：
  快速录入条 · 任务统计卡与柱状图（周/月按天、年按月）· 打卡汇总 · 数据趋势折线 ·
  **月历**（完成数徽标 + 有日记圆点，点击开当日日志）· **任务完成热力图**（近一年）·
  **内容记录热力图**（期内条目数）· 最近速记 · **日志内查询块**（期内 dataview /
  dataviewjs / tasks 块去重后委托原插件渲染，探测不到显示降级说明）
- 查询桥 `dataview-bridge.ts`：`app.plugins` 访问收口于单文件（eslint 例外写进配置，
  对外仅返回 boolean）

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下图表与月历样式、查询块在装/未装
  Dataview 与 Tasks 时的降级展示——均未在真实 vault 验证，欢迎反馈。

## 0.4.0

### 新增

- **「段落」标题区类型**：整段自由文字（非列表、非内联字段），一天一条、内容可长；
  已有内容时覆盖前确认；支持速记面板（聚合显示 + 输入条直发）
- **自动时间戳**：列表 / 段落标题区在面板开启后可选——写入自动加 `HH:mm` 前缀，
  速记面板解析并在每条记录旁显示时间；面板未开启时该选项禁用
- **速记面板条目就地编辑 / 删除**：每条记录带编辑、删除、跳转三个按钮——
  列表行原位改写（保留列表标记与时间戳）、字段条目改值 / 清值、段落整段重写 / 清空；
  行级写回前做原文校验，笔记已变化时提示刷新而不是写错行

### 修复

- 多行插入顺序算法重写（同锚点连排、标题与首行相邻），段落 / 补建行落的
  位置不再受原空行干扰

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.3.0

### 新增

- **速记面板**（命令「打开速记面板」，Thino 式内容流）：顶部输入条直发到列表标题区
  （Enter 发送 / Shift+Enter 换行），下方按天聚合「开启内容汇总面板」的文本 / 列表标题区内容
  （今天 / 昨天 / 日期分组，任务带状态符号，点击条目跳转当日日志）；近 7 / 30 天切换；
  日志文件变更防抖自动刷新
- **按标题区开启面板**：设置里文本 / 列表类型新增「开启内容汇总面板」开关
- **命令「打开快速录入」**：直接打开动作选择弹窗（可绑快捷键 / 移动端工具栏）
- **设置「恢复默认设置」**：确认后重置全部配置（只清 data.json，不动笔记内容）

### 说明

- 速记面板为原创实现（vanilla DOM），交互形态参考 Obsidian-Thino-Lite（MIT）；
  未复用其代码

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.2.0

### 变更

- **范围收缩为日日志快速录入**：移除周复盘动作与汇总视图里的复盘卡片（复盘功能另行开发）
- **标题区模型**：设置里以「日志标题」为配置单元——每个标题区选类型（打卡 / 数据 / 文本 / 列表），
  自定义标题与字段；无内联字段的标题区为列表，逐项追加内容（可配行模板，如 `- [ ] {{value}}`）
- **从模板识别**：设置里填模板笔记路径，一键按标题与内联字段重建标题区配置
- **打卡类型**：勾选式 ✔️/❌ 开关（不选不写），替代旧的「留空不写」脚本式提示——
  表单提示整体去 QuickAdd 化
- v0.1 配置自动迁移（registry.daily → 标题区；周/月与旧动作丢弃）

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。

## 0.1.0

### 新增

- **快速录入（原型）**：6 个出厂动作（每日打卡 / 数据记录 / 今日小结 / 本周复盘 /
  加一条任务 / 记一条灵感）；表单按字段类型渲染（布尔三态钮 / 数字 / 文本 / 多行），
  留空不写、覆盖前确认；写入走 `Vault.process` 原子替换，定位不到字段行时按注册表补建。
- **目标笔记自动创建**：按字段注册表生成骨架（frontmatter + 标题 + 空值字段行），
  与现有 TPL-* 日志模板同构。
- **日志汇总视图（原型）**：周 / 月 / 年期间切换；任务完成 / 新建统计、打卡率比例条、
  数值字段均值与区间；周视图可打开 / 创建本周复盘笔记。
- **i18n**：中英双语（跟随 Obsidian，可手动切换），`check:i18n` 校验键一致性。
- 解析 / 期间 / 定位 / 聚合纯函数层 + vitest 单测（含骨架 round-trip）。

### 未验证

- 移动端（iOS / Android）实机行为、明暗主题下的样式细节、与 Dataview / Tasks /
  Meta Bind / Templater 共存——均未在真实 vault 验证，欢迎反馈。
